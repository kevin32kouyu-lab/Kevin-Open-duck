# Configs

- `duck_config_tuned.json`: final tuned robot configuration copied from the Raspberry Pi backup.
- `imu_calibration_offsets.json`: readable JSON version of the IMU calibration offsets found in the runtime calibration pickle.

Use these files as robot-specific references. Recalibrate if the IMU, SD card, servo assembly, or robot structure changes.
